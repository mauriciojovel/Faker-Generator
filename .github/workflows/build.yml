name: Build and Deploy

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧺 Checkout Repository Code
        uses: actions/checkout@v3

      - name: 🛠 Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          cache: maven
          java-version: '17'

      - name: 🛠 Get project version
        run: |
          PROJECT_VERSION=$(mvn help:evaluate -Dexpression=revision -q -DforceStdout | awk -F'%' '{print $1}' | tr -d '[:space:]')
          if [[ $GITHUB_REF == refs/heads/release/* ]]; then
            CHANGE_LIST=-${GITHUB_RUN_NUMBER}-SNAPSHOT;
          else
            CHANGE_LIST=;
          fi
          PROJECT_VERSION=${PROJECT_VERSION}${CHANGE_LIST}
          echo "PROJECT_VERSION=${PROJECT_VERSION}" >> $GITHUB_ENV
          echo "CHANGE_LIST=${CHANGE_LIST}" >> $GITHUB_ENV

      - name: 📦 Build with Maven
        run: mvn clean test jacoco:report install -Drevision=${{ env.PROJECT_VERSION }} -Dchangelist=${{ env.CHANGE_LIST }} --settings .maven.xml -Dgpg.skip -Dmaven.javadoc.skip=true -B -V

      - name: 🚀 Coveralls Coverage Report Submission
        env: # Or as an environment variable
          COVERALL_TOKEN: ${{ secrets.COVERALL_TOKEN }}
        run: mvn coveralls:report -Drevision=${{ env.PROJECT_VERSION }} -Dchangelist=${{ env.CHANGE_LIST }} -DrepoToken=${{ env.COVERALL_TOKEN }} --settings .maven.xml -B -V
        if: github.event_name == 'push' # && (github.event.ref == 'refs/heads/master' || startsWith(github.event.ref, 'refs/heads/release/'))
#
#      - name: Create deploy file on master
#        run: echo "Deployment file content" > deploy.txt
#        if: github.event_name == 'push' && github.event.ref == 'refs/heads/master'
#
#      - name: Commit and push deploy file
#        run: |
#          git config --local user.email "actions@github.com"
#          git config --local user.name "GitHub Actions"
#          git add deploy.txt
#          git commit -m "Add deploy file"
#          git push
#        if: github.event_name == 'push' && github.event.ref == 'refs/heads/master'
